from django.urls import path, re_path
from . import views
# from .views import cart, checkout, single_product, shop

urlpatterns = [
    # path('cart/', cart, name='cart'),
    # path('shop/', shop, name='shop'),
    # path('checkout/', checkout, name='checkout'),
    # re_path(r'breakfast/(?P<slug>[-\w]+)/', single_product, name='single-product'),
]
